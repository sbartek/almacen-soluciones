<%= bootstrap_form_for(@ficha) do |f| %>
  <% if @ficha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ficha.errors.count, "error") %> prohibited this ficha from being saved:</h2>
      <ul>
      <% @ficha.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.text_field :codigo %>
  </div>
  <div class="form-group">
    <%= f.select :importancia, "<option>A</option><option>B</option><option>C</option><option>D</option>".html_safe %>
  </div>
  <div class="field">
    <%= f.text_area :observaciones %>
  </div>

  <!-- <div class="checkbox"> -->
  <!--   <%= f.collection_check_boxes(:subfamilia_ids, Subfamilia.all, :id, :nombre_completo) %> -->
  <!-- </div> -->
  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
