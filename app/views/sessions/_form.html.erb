<%= bootstrap_form_for(:session, url: sessions_path) do |f| %>

  <%= f.text_field :email %>

  <%= f.password_field :password %>

  <%= f.submit "Accede", class: "btn btn-large btn-primary" %>
<% end %>
